"""Tests for the Content Assistant L0 pipeline.

Written against :mod:`unittest` from the standard library so they run with no
extra dependency::

    python -m unittest discover -s content_assistant/tests -t .

They are fully deterministic: no LLM, no network, no PDF, no Marker run.
"""
